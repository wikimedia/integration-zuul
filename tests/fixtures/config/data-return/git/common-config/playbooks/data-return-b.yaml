- hosts: localhost
  tasks:
    - zuul_return:
        data:
          zuul:
            child_jobs:
              - data-return-d
            log_url: http://example.com/test/log/url/
          child:
            valueB: 'yes'
